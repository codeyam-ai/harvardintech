---
// A chapter's centered header: city title with the shared blue underline, an
// optional uppercase region line, and an optional blurb. Split out of
// ChapterPage so the page composes sub-components rather than raw markup.
interface Props {
  /** The city (chapter) or community name shown as the title. */
  name: string;
  region?: string;
  blurb?: string;
  /** The same kicker `ChapterHero` shows — "Chapter", "Chapter · Forming",
   *  "Community". Carried through the no-image fallback so a page without a
   *  hero photo still says which kind of page it is. */
  label?: string;
}
const { name, region, blurb, label } = Astro.props;
---

<header style="text-align: center; margin: var(--space-lg) 0 var(--space-xl);">
  {
    label ? (
      <p style="color: var(--color-faint); text-transform: uppercase; letter-spacing: 0.18em; font-size: 0.8rem; font-weight: 600; margin: 0 0 var(--space-sm);">
        {label}
      </p>
    ) : null
  }
  <h1 class="s-title" style="margin-bottom: var(--space-md);">{name}</h1>
  {
    region ? (
      <p style="color: var(--color-faint); text-transform: uppercase; letter-spacing: 0.08em; font-size: 0.85rem; margin: 0;">
        {region}
      </p>
    ) : null
  }
  {
    blurb ? (
      <p style="color: var(--color-muted); font-size: 1.1rem; margin: var(--space-md) auto 0; max-width: var(--text-width);">
        {blurb}
      </p>
    ) : null
  }
</header>
